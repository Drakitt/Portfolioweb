# Optional. Set after the first deployment to the public origin (no trailing slash).
# Local development works without this value.
# Example after Vercel deploy: https://your-project.vercel.app
# NEXT_PUBLIC_SITE_URL=
