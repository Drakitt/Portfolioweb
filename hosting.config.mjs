/**
 * GitHub Pages project-site hosting.
 *
 * Production origin: https://drakitt.github.io/portfolio
 * This is not a user-root site and does not use a custom domain.
 *
 * `next.config.mjs` reads these values. Application code should use
 * `src/lib/hosting.ts` and `src/lib/site-url.ts` instead of hardcoding
 * `/portfolio`.
 */
export const GITHUB_PAGES_BASE_PATH = '/portfolio';
export const GITHUB_PAGES_SITE_URL = 'https://drakitt.github.io/portfolio';
